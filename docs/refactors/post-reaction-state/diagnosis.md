# Diagnosis — post-reaction-state

## 특성화 테스트 결과 (2026-07-20, `apps/web` Jest + React Testing Library 도입 후, #36)

- `apps/web/src/components/post/PostCard.test.tsx` — 5개 테스트, 전부 PASS
- `apps/web/src/hooks/post/usePostReactions.test.ts` — 6개 테스트, 전부 PASS(버그 특성화 포함)
- `pnpm test`에 `apps/web`이 처음으로 포함됨(이전에는 `apps/api`만 실행됨)

### Fact — 확인된 정상 동작

- 카드에서 좋아요를 누르면 즉시 낙관적으로 반영되고, 성공 시 `usePostReactionOverridesStore.likesByPostId`에 기록된다.
- 좋아요 API가 실패하면 로컬 상태와 전역 override 모두 이전 값으로 롤백된다.
- 비로그인 사용자는 좋아요 버튼이 비활성화되고 API가 호출되지 않는다.
- 한쪽 화면(카드 또는 모달)이 먼저 override를 남기면, 다른 화면은 마운트/재계산 시 그 값을 그대로 반영한다 — "어느 한쪽이 store에 쓰면 다른 쪽이 읽는다"는 동기화 자체는 설계대로 동작한다.
- 서버가 read-after-write consistency를 보장하는 정상 케이스에서는 댓글 작성 후 `commentsByPostId`가 올바르게 갱신된다.

### Fact — 재현된 문제

1. **댓글 작성 직후 refetch가 방금 쓴 댓글을 지울 수 있음** → 버그 이슈 [#39](https://github.com/Jae-Hyuk-Jang/web17-boostcamp-vibr/issues/39)(bug 라벨, 이 리팩터링과 분리). `submitComment`가 tmp id를 서버 id로 교체한 직후 `refetchComments()`를 부르는데, `mergeComments`는 "아직 tmp인" 로컬 댓글만 보호하므로 이미 교체된 댓글은 서버 스냅샷이 늦으면 사라진다.
2. **카드와 모달의 인증 판단 소스가 다름**. `PostCard`는 전역 `useAuthStore.isAuthenticated`, `usePostReactions`(모달)는 자체 `authMe()` 호출로 각자 판단한다. `authMe()` 응답 전에 좋아요를 누르면 실제 로그인 사용자여도 조용히 무시된다.

---

## 진단 원칙 적용

- **파일 길이는 신호일 뿐**: `PostCardDetailModal.tsx`(396줄), `usePostReactions.ts`(362줄)가 크다는 사실 자체보다, 그 안에 서로 다른 변경 이유가 몇 개나 섞여 있는지를 근거로 삼는다(아래 탐색 항목 참고).
- **근거 등급**: 아래 모든 항목에 Fact(코드로 직접 확인) / Inference(여러 사실의 해석) / Hypothesis(미검증) 등급을 표시한다.

## 탐색 항목 체크 (baseline.md 근거 기반)

| 항목             | 관찰                                                                                                                                                   | 등급      |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- |
| 책임 응집도      | `usePostReactions`가 좋아요 토글+댓글 CRUD+댓글 폴링+자체 인증 판단을 한 훅에서 수행                                                                   | Fact      |
| 결합도           | 좋아요 override 하나를 바꾸려면 `PostCard.tsx`, `usePostReactions.ts` 최소 2곳을 함께 고쳐야 함(실제로 로직이 중복 구현돼 있음)                        | Fact      |
| 상태 소유권 중복 | "좋아요가 눌렸는가"의 진실 공급원이 로컬 state(카드/모달 각자) + 전역 override 두 겹으로 존재                                                          | Fact      |
| 경계 명확성      | 좋아요/댓글은 훅 경유, 본문수정/삭제는 컴포넌트가 스토어를 직접 호출 — 접근 경로가 반응 타입마다 다름                                                  | Fact      |
| 테스트 이음새    | #36에서 특성화 테스트 11개 확보 — 이 영역은 이제 안전망이 있음                                                                                         | Fact      |
| 오류·동시성      | 낙관적 갱신 롤백은 있으나(#36 확인), 낙관적→서버 확정 전환 시점의 재검증 설계가 취약(#39)                                                              | Fact      |
| 데이터 계약 혼재 | `PostCard`의 `postForActions`가 서버 `Post` DTO에 로컬 optimistic 필드(`isOptimisticLiked` 등)를 얹어 화면 모델처럼 씀 — DTO와 화면 모델 경계가 흐릿함 | Inference |
| 운영성           | 좋아요/댓글 상태 변경에는 로그·메트릭 없음(모달의 UX 로깅은 별개 관심사)                                                                               | Fact      |
| 변경 이력        | `PostCardDetailModal.tsx`가 최근 100개 커밋 중 3회 변경(hot spot)                                                                                      | Fact      |
| 죽은 추상화      | 스토어의 `clearLikeOverride`/`clearCommentOverride`가 정의만 되어 있고 어디서도 호출되지 않음                                                          | Fact      |

---

## 후보

### 후보 1 — 반응 상태 접근 계층 부재 (좋아요/댓글 소유권·동기화 캡슐화)

**증상 → 원인 체인**

```
증상: 좋아요 토글 로직이 PostCard/usePostReactions 두 곳에 중복 구현되고,
      인증 판단 소스까지 서로 다르다.
  ↓ 왜?
직접 원인: usePostReactionOverridesStore에 대한 접근(읽기 병합 규칙, 쓰기, 인증 체크)을
           캡슐화하는 단일 진입점이 없어, 각 소비자가 매번 자기 방식대로 구현한다.
  ↓ 왜?
구조 원인: 반응 타입별 전담 훅(seam)이 애초에 설계되지 않았다 — store는 여러 화면 간
           동기화라는 제 역할은 하지만, 접근 통제 없이 누구나 직접 import해서 read/write할 수 있다.
```

**우선순위 평가** (1~5)

| 기준          | 점수 | 근거                                                                                          |
| ------------- | ---- | --------------------------------------------------------------------------------------------- |
| 변경 빈도     | 4    | `PostCardDetailModal` hot spot, 브리프의 "기대 효과"에 새 반응 타입(북마크 등) 추가 계획 명시 |
| 결함 위험     | 5    | 이미 실제 버그 2건 확인(#39, 인증 소스 불일치)                                                |
| 영향 범위     | 4    | 6개 파일에 파급                                                                               |
| 근거 강도     | 5    | 전부 Fact, 코드로 직접 확인됨                                                                 |
| 안전망 가능성 | 5    | 특성화 테스트 11개 이미 확보(#36)                                                             |
| 구현 비용     | 3    | 점진 전환 가능(카드→모달 순으로 훅 교체)하나 6개 파일을 순차적으로 다뤄야 함                  |

**YAGNI**: 향후 반응 타입 추가(북마크 등)가 브리프에 이미 예고되어 있어 "그렇다"에 해당 — 지금 정리할 가치 있음.

### 후보 2 — `PostCardDetailModal`의 책임 과다 (반응 상태 외 관심사 분리)

반응 상태 반영 외에도 UX 로그 수집, 모바일↔데스크톱 리사이즈 시 라우팅 전환, 스와이프-투-디스미스가 396줄 안에 함께 있음(Fact).

| 기준          | 점수 | 근거                                                                     |
| ------------- | ---- | ------------------------------------------------------------------------ |
| 변경 빈도     | 3    | hot spot이지만 변경 이유가 반응 상태 외에도 다양해 이 후보 자체로는 애매 |
| 결함 위험     | 2    | 실제 버그로 이어진 사례 없음(Inference 수준)                             |
| 영향 범위     | 2    | 사실상 파일 1개, 국지적                                                  |
| 근거 강도     | 3    | 다중 관심사는 Fact지만 "문제를 일으켰다"는 근거는 약함                   |
| 안전망 가능성 | 2    | 로깅/라우팅/제스처 부분은 특성화 테스트가 없음(#36은 반응 상태만 다룸)   |
| 구현 비용     | 3    | —                                                                        |

**YAGNI**: 이 브리프가 원래 겨냥한 "반응 상태 소유권" 문제와는 결이 다름 — 지금 사이클에서는 보류.

### 후보 3 — `usePostReactions` 자체 책임 분리(좋아요/댓글/인증)

후보 1의 해법(반응 타입별 전담 훅 추출)을 적용하면 자연히 해소되는 하위 산출물에 가까워 독립 후보로 두지 않고 후보 1에 통합.

---

## 권장 후보

**후보 1(반응 상태 접근 계층 부재)을 권장합니다.** 근거 강도·결함 위험·안전망 가능성 모두 가장 높고, 이미 발견된 버그(#39, 인증 소스 불일치)의 구조적 원인과도 직접 맞닿아 있습니다. 후보 2는 실재하지만 이번 브리프의 범위와 결이 달라 별도 사이클(예: `player-detail-modal-responsibility` 같은 새 브리프)로 남겨두는 것을 제안합니다.

**Out of Scope 확인**: #39(댓글 소실 버그)와 인증 소스 불일치는 "구조 원인"으로는 후보 1에 포함되지만, 실제 **동작을 바꾸는 수정 자체**는 이번 구조 개선 사이클에 섞지 않습니다. 훅을 추출할 때 기존 `submitComment`/`toggleLike` 로직은 동작 변경 없이 그대로 옮기고, #39 수정은 별도 이슈에서 진행합니다.

AI의 추천과 사용자의 최종 선택은 다를 수 있습니다. 다른 후보를 선택하시면 그 근거를 이 문서에 추가하겠습니다. 최종 선택권은 항상 사용자에게 있습니다.

---

**[GATE 1]** 이번 사이클에서 다룰 후보를 확정해주세요.

1. **후보 1(반응 상태 접근 계층 부재)로 진행** — 추천안입니다.
2. **후보 2(PostCardDetailModal 책임 분리)로 진행** — 범위가 다르니 새 브리프로 분리하는 걸 권하지만, 원하시면 이 사이클에 포함할 수도 있습니다.
3. 둘 다 아니고 다른 방향을 원하시면 말씀해주세요.

확정해주시면 단계 2(목표 인터뷰, `brief-fixed.md`)로 진행하겠습니다.

---

## GATE 1 승인 기록

**후보 ①(반응 상태 접근 계층 부재)로 확정.** 후보 ②는 사용자가 별도로 중요성을 확인했고, 범위를 두 갈래로 나눠 백로그에 기록:

- [#41](https://github.com/Jae-Hyuk-Jang/web17-boostcamp-vibr/issues/41) — `PostCardDetailModal` 책임 분리(이 문서의 후보 ② 그대로)
- [#42](https://github.com/Jae-Hyuk-Jang/web17-boostcamp-vibr/issues/42) — `components/` 전반의 훅 기반 합성 패턴 정립(더 넓은 범위, 사용자가 이번에 새로 제기)

단계 2(목표 인터뷰)로 진행합니다.
